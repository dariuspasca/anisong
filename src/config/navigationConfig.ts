import type { NavigationConfig } from "@/types"

export const navigationConfig: NavigationConfig = {
  navigationItems: [
    {
      title: "Dashboard",
      href: "/dashboard",
      icon: "dashboard",
    },
  ],
}
